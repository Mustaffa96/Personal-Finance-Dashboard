{
  "name": "personal-finance-dashboard",
  "version": "1.0.0",
  "description": "A web-based personal finance tracker with a clean, interactive interface",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter client dev & pnpm --filter server dev",
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev",
    "build": "pnpm --filter client build & pnpm --filter server build",
    "start": "pnpm --filter client start & pnpm --filter server start",
    "lint": "pnpm --filter client lint"
  },
  "keywords": [
    "finance",
    "dashboard",
    "nextjs",
    "honojs",
    "typescript",
    "mongodb"
  ],
  "author": "Mustaffa96",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
