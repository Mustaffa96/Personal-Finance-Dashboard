# Server configuration
PORT=5000
HOST=0.0.0.0
LOG_LEVEL=info

# MongoDB configuration
# Using localhost:27017 directly in the code
MONGODB_DB_NAME=finance_dashboard

# JWT configuration
JWT_SECRET=your-secret-key-change-this-in-production
TOKEN_EXPIRATION=1d

# CORS configuration
CORS_ORIGIN=http://localhost:3001
